<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ByaheMoto" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.ByaheMoto" parent="Base.Theme.ByaheMoto" />

    <style name="MyHintTextAppearance" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">14sp</item> <!-- Adjust the text size as needed -->
        <item name="android:textColor">@color/field</item> <!-- Optional: Adjust text color -->
    </style>
    <style name="customBot">
        <item name="android:fontFamily">@font/montserrat</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">12sp</item>
    </style>
</resources>